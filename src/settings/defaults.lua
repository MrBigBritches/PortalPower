PortalPower.Settings.defaults = {
  profile = {
    debugEnabled = true,

    tooltip = PortalPower.Enum.Options.TOOLTIP.DEFAULT,

    leftClick = 'portal',
    rightClick = 'teleport',

    scale = 1,
    spacing = 4,
    direction = PortalPower.Enum.Options.DISPLAY.HORIZONTAL,
    xOffset = 10,
    yOffset = -10,

    destinations = {
      order = 1,
    },
  },
}
